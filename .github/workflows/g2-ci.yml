name: g2-ci

on:
  pull_request:

jobs:
  g2-gate:
    name: g2-gate
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: product-test
        run: python3 -m unittest test_add.py -v
